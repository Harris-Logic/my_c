#include<stdio.h>

int main(){
    // int year;
    // scanf("%d",&year);
    // if(year%4==0&&year%100!=0||year%400==0) printf("yes");
    // else printf("not yunnian");
    while(1);

    return 0;
}

//判断核心是year%4==0&&year%100!=0||year%400==0，文科内容