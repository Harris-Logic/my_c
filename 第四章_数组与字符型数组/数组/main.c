#include<stdio.h>

void print(int a[],int len){//这个len只能你自己想办法搞，要么自变量，要么定死的数值（不变量）

}
int main(){
    
    // sizeof(a)/sizeof(int)；//查看 数组里有几个人的思路，但是是错误的思路。数组的成员数不会传递。只能你自己传递 数组有几个人 这个事情
    

    return 0;
}